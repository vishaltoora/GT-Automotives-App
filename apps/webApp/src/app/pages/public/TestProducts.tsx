import { Typography, Container } from '@mui/material';

export const TestProducts = () => {
  return (
    <Container>
      <Typography variant="h1">Products Page</Typography>
      <Typography>This is a test products page</Typography>
    </Container>
  );
};